# Spooled Go SDK — Agent Knowledge Base

Module tag **v1.1.0**; `internal/version.Version` + User-Agent `spooled-go/1.1.0 (GOOS; GOARCH)`.

## Surface
REST + worker (including real `UpdateProgress`) + realtime + full gRPC. Auth REST Bearer; gRPC `x-api-key`.
